# Busybox httpd configuration for lab-tester test VM
# Serves the identity page on port 80
# Place this at /etc/httpd.conf or pass via httpd -c <path>

# Home directory for static files
H:/var/www/localhost/htdocs

# Default MIME type
*.html:text/html
*.txt:text/plain
*.json:application/json
